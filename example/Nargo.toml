[package]
name = "example"
type = "bin"
authors = [""]
compiler_version = ">=0.31.0"

[dependencies]
rsa = { path = "../lib" }
bignum = { tag = "v0.3.0", git = "https://github.com/noir-lang/noir-bignum" }
